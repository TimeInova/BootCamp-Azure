version: '3'

networks:
  bootcamp_ici:

services:
  mongo:
    image: mongo
    ports:
      - 27017:27017
    networks:
      - bootcamp_ici
  
  rabbitmq:
    image: rabbitmq:3.9-management
    ports:
      - 5672:5672
      - 15672:15672
    networks:
      - bootcamp_ici